name: Evergreen Auto Checkin

on:
  schedule:
    - cron: '5 15 * * *' # 한국 시간 오전 12시 05분 (UTC 15:05)
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write   # workflow 권한 필요 없음

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
          token: ${{ secrets.GH_PAT }}

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: pip install selenium webdriver-manager

      - name: Run script
        env:
          USER_ID: ${{ secrets.USER_ID }}
          USER_PW: ${{ secrets.USER_PW }}
        run: python run.py

      - name: Commit and Push changes
        env:
          MY_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          git config --global user.name "GitHub Actions Bot"
          git config --global user.email "actions@github.com"
          
          # 변경된 모든 파일 커밋 (워크플로우 파일 제외)
          git add . ':!./.github/workflows/*'
          git commit -m "📝 출석 기록 업데이트" || echo "No changes"
          
          git push https://x-access-token:${MY_TOKEN}@github.com/${{ github.repository }}.git main

