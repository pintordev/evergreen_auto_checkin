name: Evergreen Auto Checkin

on:
  schedule:
    # 한국 시간(KST) 오전 00:05는 UTC 기준 15:05입니다.
    - cron: '5 15 * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: pip install selenium webdriver-manager

      - name: Run script
        env:
          USER_ID: ${{ secrets.USER_ID }}
          USER_PW: ${{ secrets.USER_PW }}
        run: python run.py

      - name: Commit and Push changes
        run: |
          git config --global user.name "GitHub Actions Bot"
          git config --global user.email "actions@github.com"
          
          # 1. 원격 저장소의 최신 상태를 가져옵니다.
          git fetch origin main
          
          # 2. 오직 README.md 파일만 서버의 최신 버전으로 맞춥니다.
          git checkout origin/main -- README.md || echo "신규 파일임"
          
          # 3. 변경사항을 반영합니다.
          git add README.md
          git commit -m "📝 출석 기록 업데이트" || echo "변경사항 없음"
          
          # 4. README.md만 골라서 업데이트합니다.
          git push origin main
