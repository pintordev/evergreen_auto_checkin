name: Evergreen Auto Checkin

on:
  schedule:
    # 한국 시간(KST) 오전 00:05는 UTC 기준 15:05입니다.
    - cron: '5 15 * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: pip install selenium webdriver-manager

      - name: Run script
        env:
          USER_ID: ${{ secrets.USER_ID }}
          USER_PW: ${{ secrets.USER_PW }}
        run: python run.py

      - name: Commit and Push changes
        env:
          # 아까 등록한 시크릿 토큰을 사용합니다.
          GH_PAT: ${{ secrets.GH_PAT }}
        run: |
          git config --global user.name "GitHub Actions Bot"
          git config --global user.email "actions@github.com"
          
          git add README.md
          git commit -m "📝 출석 기록 업데이트" || echo "No changes"
          
          # 기본 GITHUB_TOKEN 대신 우리가 만든 GH_PAT를 주소에 넣어 푸시합니다.
          git push https://x-access-token:${GH_PAT}@github.com/${{ github.repository }}.git main --force
