name: Evergreen Auto Checkin

on:
  schedule:
    # 한국 시간(KST) 오전 00:05는 UTC 기준 15:05입니다.
    - cron: '5 15 * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: pip install selenium webdriver-manager

      - name: Run script
        env:
          USER_ID: ${{ secrets.USER_ID }}
          USER_PW: ${{ secrets.USER_PW }}
        run: python run.py

      - name: Commit and Push changes
        run: |
          git config --global user.name "GitHub Actions Bot"
          git config --global user.email "actions@github.com"
          
          # attendance.log 파일만 추가합니다.
          git add attendance.log
          
          # 변경사항이 있을 때만 커밋하고 푸시합니다.
          if git diff --staged --quiet; then
            echo "변경사항 없음"
          else
            git commit -m "📝 출석 기록 업데이트"
            # 특정 파일만 지정해서 푸시하여 설정을 건드리지 않음을 증명합니다.
            git push origin main
          fi
