name: Evergreen Auto Checkin

on:
  schedule:
    # 한국 시간(KST) 오전 00:05는 UTC 기준 15:05입니다.
    - cron: '5 15 * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: pip install selenium webdriver-manager

      - name: Run script
        env:
          USER_ID: ${{ secrets.USER_ID }}
          USER_PW: ${{ secrets.USER_PW }}
        run: python run.py

      - name: Commit and Push changes
        run: |
          git config --global user.name "GitHub Actions Bot"
          git config --global user.email "actions@github.com"
          
          # 1. 서버의 최신 상태를 가져오고 현재 변경사항과 합칩니다.
          git pull origin main --rebase
          
          # 2. README.md 파일만 스테이징합니다. (설정 파일은 절대 건드리지 않음)
          git add README.md
          
          # 3. 변경사항이 있을 때만 커밋합니다.
          git commit -m "📝 출석 기록 업데이트" || echo "No changes to commit"
          
          # 4. 푸시합니다. (--force를 빼서 보안 검사를 통과합니다)
          git push origin main
